# Prototipos iniciales

Maquetas sueltas anteriores al proyecto actual. Estaban en la raíz del
repositorio de GitHub, subidas una a una por la interfaz web, antes de que
existiera el código de `src/` y `backend/`.

**No forman parte de la aplicación.** Vite no las compila, no importan nada del
proyecto y no se despliegan. Se conservan como referencia de diseño: muestran la
intención visual y el flujo de cada panel antes de implementarlos.

| Archivo | Equivalente vivo hoy |
|---|---|
| `univirtual-login-v2.tsx` | `src/pages/Login.tsx` |
| `admin-dashboard-univirtual.tsx` | paneles de `src/components/admin/` |
| `panel-docente-completo (2.1).tsx` | paneles de docente en `src/` |
| `panel_estudiante_univirtual-FINAL2.tsx` | paneles de estudiante en `src/` |

Al leerlos, ten en cuenta que sus datos son inventados: los `password: 'prog2024'`
que aparecen son contraseñas de ejemplo de una clase virtual dentro de la
maqueta, no credenciales de ningún sistema.
