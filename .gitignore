# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.production.local
.env.development.local

# Archivos subidos en tiempo de ejecucion
uploads/
backend/uploads/

# Lista de secretos historicos que la bitacora debe redactar (contiene
# credenciales rotadas en texto plano: nunca debe subirse)
scripts/.bitacora-secretos.txt

# Estado incremental de la bitacora: depende de mtime, que cambia en cada
# maquina. Versionarlo solo provocaria regeneraciones completas innecesarias.
docs/bitacora/.estado.json

# Respaldos generados por scripts/backup.sh. El volcado de la base contiene
# datos personales reales del alumnado y este repositorio es publico: nunca
# debe subirse. Los .tsx sueltos que ya habia en backups/ si se conservan.
backups/virtualuni_backup_*.sql
backups/virtualuni_backup_*.sql.gz
backups/virtualuni_bitacora_*.tar.gz
